@model List<Kisi>
@{
    ViewData["Title"] = "About";
}
<h2>@ViewData["Title"]</h2>
<h3>@ViewData["Message"]</h3>

<p>Use this area to provide additional information.</p>
<p>@ViewBag.Hello</p>
<ul>
    @foreach (var item in Model)
    {
        <li name="@item.Id">@item.Ad @item.Soyad</li>
    }
</ul>